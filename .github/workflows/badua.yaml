name: Badua-Coverage

on:
  push:
    branches:
      - "ISW2"
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build and analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          java-version: 8.0.372
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Clone and build ba-dua repository
        run: |
          git clone https://github.com/saeg/ba-dua
          cd ba-dua
          git checkout d2401f0
          mvn clean install
          cd ..

      - name: Build and analyze zookeeper project
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mvn clean verify -P badua -Dfile.encoding=UTF-8

      - name: Print target directory structure
        run: |
          echo "Printing target directory structure"
          find target -type d || true

      - name: Ensure common directory exists
        run: mkdir -p target/classes/org/apache/zookeeper/common

      - name: Print contents of common directory
        run: |
          echo "Contents of target/classes/org/apache/zookeeper/common"
          ls -la target/classes/org/apache/zookeeper/common || true

      - name: Create reports directory if not exists
        run: mkdir -p reports/surefire-reports

      - name: Copy Surefire reports to reports directory
        run: cp -R target/surefire-reports/* reports/surefire-reports/

      - name: Create badua reports directory if not exists
        run: mkdir -p zookeeper-server/target

      - name: Copy BaDua reports to target directory
        run: cp -R target/badua.xml zookeeper-server/target/

      - name: Upload report
        uses: actions/upload-artifact@v3
        with:
          name: BaDuaCoverage_Report
          path: zookeeper-server/target/badua.xml
          if-no-files-found: error
